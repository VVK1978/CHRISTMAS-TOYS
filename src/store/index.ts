import { bg } from './bg';
import data from './data';
import {
  initialStorage,
  setStorage,
  getStorage,
  addBookmark,
  removeBookmark,
  getBookmarks,
  resetStorage,
  getSort,
  setSort,
  initialSettings,
  setSettings,
  getSettings,
  resetSettings,
} from './storage';
import { trees } from './trees';

export {
  data,
  trees,
  bg,
  initialStorage,
  initialSettings,
  setStorage,
  getStorage,
  addBookmark,
  removeBookmark,
  getBookmarks,
  resetStorage,
  getSort,
  setSort,
  setSettings,
  getSettings,
  resetSettings,
};
